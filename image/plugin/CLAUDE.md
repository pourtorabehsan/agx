# Rules

- Never push directly to the default branch. Always create a feature branch and open a PR.
- Use `$AGX_BRANCH_PREFIX` as the branch prefix when set (e.g. `$AGX_BRANCH_PREFIX/short-description`).
